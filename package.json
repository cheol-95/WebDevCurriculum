{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "clean": "lerna clean --yes && lerna bootstrap",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
