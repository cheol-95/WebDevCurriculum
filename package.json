{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean --yes && lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test --scope notepad-server"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
